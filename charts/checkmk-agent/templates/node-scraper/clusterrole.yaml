apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "checkmk-agent.labels" . | nindent 4 }}
  name: {{ include "checkmk-agent.fullname" . }}-node-scraper
rules:
  - apiGroups: [""]
    resources: ["nodes/stats", "nodes/healthz"]
    verbs: ["get"]
